local snax = require "snax"

local player = { uid = 0}

function init(uid)
    -- init player, get player from db    
end

function exit()

end

function reaspone.acquice_items(items)
    -- body
    return true
end

function response.hello( ... )
    -- body
    print(uid, "hello")
end